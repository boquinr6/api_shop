source 'https://rubygems.org'

ruby '3.3.8' 
gem 'rails', '~> 7.1' # Or the latest stable Rails version if you prefer
gem 'pg'              # PostgreSQL adapter
gem 'rspec-rails'     # For testing with RSpec
gem 'byebug', platforms: [:mri, :mingw, :x64_mingw]
gem 'dotenv-rails'    # went with loading env vars from .env


# Use Puma as the app server
gem 'puma', '~> 6.0'

# Reduces boot times through caching; required for zeitwerk mode.
gem 'bootsnap', require: false

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
